import 'package:flutter/material.dart';

final class FoodScreen extends StatelessWidget {
  const FoodScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return BaseScreen(
      appBar: AppBar(),
      body: const Text('Start'),
    );
  }
}
